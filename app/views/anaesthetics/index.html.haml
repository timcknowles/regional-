%p#notice= notice
%h1 Anaesthetics

%table.table.table-striped.table-responsive
  %thead
    %tr
      %th Patient
      %th Doctor
      %th grade
      %th Date and time
      %th Intervention
      %th Indication
      %th Attempts
      %th Complications
      %th View
      %th Edit
      %th Delete
      %th{:colspan => "3"}
  %tbody
    - @anaesthetics.each do |anaesthetic|
      %tr
        %td= anaesthetic.patient.full_name
        %td= anaesthetic.doctor.full_name
        %td= anaesthetic.doctor.grade
        %td= anaesthetic.date_and_time.strftime("%m/%d/%Y %H:%M")
        %td= anaesthetic.intevention
        %td= anaesthetic.indication
        %td= anaesthetic.no_attempts
        %td= anaesthetic.complications
        %td= link_to 'Follow Up', [@patient, anaesthetic], :class => 'btn btn-mini btn-success'
        -#%td= link_to 'Edit', edit_patient_anaesthetic_path(@patient, anaesthetic), :class => 'btn btn-mini btn-warning'
        %td= link_to 'Delete', [@patient, anaesthetic], method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini btn-danger'
%br/
= link_to 'New Anaesthetic', new_anaesthetic_path

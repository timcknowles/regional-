= simple_form_for(patient) do |f|
  = f.error_notification
  .field
    = f.input :first_name

  .field
    = f.input :last_name

  .field
    = f.input :DOB

  .field
    = f.input :NHS_No

  .field
    = f.input :MRN

  .actions
    = f.submit nil, label:'Save', :class => 'btn btn-primary'
